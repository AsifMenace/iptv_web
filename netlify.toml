[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA-friendly: everything else just serves the static files
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/xtream-proxy"
  status = 200
